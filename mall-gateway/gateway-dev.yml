server:
  port: 8010

spring:
  application:
    name: mall-gateway
  devtools:
    restart:
      enabled: true
    freemarker:
      cache: false
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
    consul:
      host: localhost
      port: 8500
  rabbitmq:
    host: 127.0.0.1
    port: 15672
    username: mall
    password: mall
